import {
} from './constants';

const initialState = {
  blog: {},
  pageContent: '',
};

export function reducer(state = initialState, action) {
  switch (action.type) {
  default:
    return state;
  }
}
